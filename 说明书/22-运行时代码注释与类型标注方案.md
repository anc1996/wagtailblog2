# 运行时代码注释与类型标注方案

## 1. 背景与现状证据

`wagtailblog3/apps` 同时包含运行时代码、测试和迁移文件。首批检查发现，Markdown 导入和内容搜索链路虽然已有部分中文注释，但函数参数、返回值、异常边界和类职责说明不完整；解析、批量重建、检查点、租约和失败补偿逻辑不易审阅。

本方案用于分阶段补齐运行时代码的可维护性说明。它不是一次性重写整个 `apps` 目录，也不把迁移和测试文件纳入注释改造范围。

### 员工执行绑定

本方案是仓库“员工”（所有实际修改文件的 agent）的后端运行时代码交付标准。`backend` 角色必须在编码前阅读本方案；其他角色只要修改 `wagtailblog3/apps` 运行时代码，也必须遵守本方案的中文注释、类型标注、行为不变和验证要求。该绑定不新增独立执行角色，也不改变现有专业分工或生产授权边界。

员工交付时必须能提供：模块职责说明、非显而易见函数的参数/返回/异常/副作用契约、复杂算法的不变量与失败补偿顺序、未覆盖边界，以及 `compileall`、相关测试和 `git diff --check` 的精确结果。缺少任一项时，主 agent 或审查者应退回该批次。

## 2. 目标与非目标

目标：

- 为运行时函数补充能够由实现和调用关系确认的输入、输出类型。
- 为公开服务函数、复杂内部算法和状态类增加中文文档字符串。
- 说明解析边界、状态检查点、租约/重试、数据一致性和失败补偿等业务约束。
- 让维护者可以从函数声明和文档直接判断调用契约、异常编码和副作用范围。

非目标：

- 不修改业务行为、数据库结构、迁移、测试断言、配置、队列或服务 unit。
- 不为简单赋值、显而易见的框架转发、生成代码和迁移文件增加逐行注释。
- 不凭猜测引入宽泛类型、改变公共接口或顺手重构相邻代码。
- 不在本任务中发布生产、执行数据修复、创建索引或修改页面内容。

## 3. 注释与类型标注规范

### 3.1 函数和方法

- 参数和返回值优先使用现有领域类型、`Mapping`、`Sequence`、`Iterator`、`tuple` 等准确类型；无法确认时保留现有接口并在文档中说明约束，不用 `Any` 掩盖不确定性。
- 中文文档字符串按需要包含“参数”“返回”“异常”“副作用”小节。
- 文档说明业务规则和调用者必须遵守的前置条件，不重复代码本身已经表达的简单分支。
- 复杂函数说明关键不变量、边界条件、失败补偿顺序及必要的时间/空间复杂度；例如倒序替换必须保持原始偏移有效，批量写入成功后才能推进 checkpoint，删除页面失败时不得继续删除其 Mongo 草稿指针和媒体依赖。

### 3.2 类和数据对象

- 异常类说明稳定错误编码的用途，不记录敏感原文。
- 数据类说明字段含义、状态所有权和生命周期，尤其是重建批次、投递租约和导入结果。
- 解析器类说明它负责观察的 HTML/Markdown 层级，以及为何忽略脚本、样式或表格外内容。

### 3.3 中文和编码

新增或修改的注释、文档字符串使用中文；保持文件现有编码，禁止把编码修复顺手扩大为无关格式化。每个 diff 均需通过 `git diff --check`。

### 3.4 标准范例：`blog/admin_image_upload.py`

后续运行时代码统一参照 [`wagtailblog3/apps/blog/admin_image_upload.py`](../wagtailblog3/apps/blog/admin_image_upload.py) 的注释密度和审阅方式。这个文件不是要求所有模块复制相同文字，而是作为结构与深度范例：

- 模块顶部用中文说明模块职责、输入到输出的主流程，以及关键派生步骤或数据边界；不写与实现无关的背景介绍。
- 每个函数和方法的声明都标注可由调用点确认的参数类型和返回类型。Django/Wagtail 动态模型或请求对象暂时只能使用 `Any` 时，必须在文档中写明实际需要的属性和调用约束，不能用 `Any` 隐藏未知契约。
- 每个非显而易见的函数都使用中文 docstring，按需要包含“参数”“返回”“异常”“副作用”“处理边界”等小节；公开入口还要说明装饰器、认证、HTTP 方法和内部权限检查之间的关系。
- 遇到算法时说明输入、输出、不变量、边界和停止条件。例如 collection 选择要说明配置/请求/单一可写集合的优先级，标题去重要说明去扩展名、长度上限、随机后缀、冲突检查和最多五次重试。
- 遇到异常补偿或多阶段保存时说明顺序和所有权。例如原图保存成功后 rendition 只是可重建派生资源，预览失败记录日志但不能让客户端重试整次上传，以免重复创建原图。
- 只在代码不能直接表达“为什么”的位置增加中文行内注释；不为简单赋值、框架调用和显而易见的分支逐行复述代码。
- 注释、类型标注和 docstring 不得改变查询、事务、保存顺序、错误码、日志字段、返回结构或外部服务副作用；每个批次必须用原有测试证明行为未变。

完成一个文件后，审阅者应能只看模块说明、函数签名和关键 docstring，回答“谁调用、传什么、返回什么、何时失败、失败后是否已产生副作用、复杂逻辑为什么这样写”这六个问题。

## 4. 分阶段范围

### 第一批：解析与搜索可靠性链路

- `wagtailblog3/apps/blog/services/markdown_import_parser.py`
- `wagtailblog3/apps/blog/services/markdown_import_service.py`
- `wagtailblog3/apps/search/services/rebuild.py`
- `wagtailblog3/apps/search/services/delivery.py`

首批优先覆盖 Markdown/HTML 媒体解析、StreamField 组装、索引批量重建、MySQL/Mongo/Elasticsearch 一致性检查、投递租约、重试和死信处理。

### 后续批次

按依赖和风险从服务层、任务层、API 层到模型辅助代码推进。每批只处理可独立验证的文件集合；测试和迁移文件保持不改。若某函数的真实输入输出无法从调用点确认，先记录待确认项，不强行标注。

## 5. 实施步骤与验收门禁

1. 读取目标文件、调用点和对应测试，建立函数/类清单。
2. 只补类型标注和中文文档字符串；不改变表达式、查询、事务边界、异常处理和日志内容。
3. 执行首批文件语法检查，再执行 Django system check。
4. 运行 Markdown 导入、搜索重建、搜索投递相关测试；测试失败时先判断是否由标注或导入变化造成，不以修改测试掩盖问题。
5. 执行 `git diff --check`，检查修改文件清单和新增导入。
6. 在本文件“实施记录”中记录实际文件、精确命令、结果、未执行项和回滚点。

验收标准：

- 目标函数的参数和返回契约可由签名及中文文档共同审阅。
- 复杂算法的边界、不变量和异常补偿顺序有明确说明。
- 相关测试行为不变，Django check 通过，无迁移变化。
- 不产生数据库、MongoDB、Elasticsearch、媒体或消息队列写入。

## 6. 与 `systemctl.md` 的服务影响对照

`systemctl.md` 规定 `wagtailblog3.service`、maintenance Worker、Beat 和 Filebeat 的职责、队列和重启顺序。本方案只修改 Python 类型标注和中文文档字符串，不修改这些服务的 unit、环境文件、socket、端口、队列、定时任务或日志采集配置。

因此：

- 测试阶段不启动、停止或重启任何生产服务。
- 生产阶段不需要部署本方案；即使后续将代码提交发布，也必须先完成测试门禁，再按代码变更实际影响决定是否重启服务。
- 搜索投递和重建虽然由现有 maintenance Worker/Beat 调用，但本方案必须用对应测试证明运行时行为不变，不能以“文档-only”代替测试。
- `systemctl.md` 本身无需更新；只有服务 unit、环境变量、端口、队列或重启顺序发生变化时才需同步修改。

## 7. 数据、安全与回滚

本方案不产生迁移，不写入数据库或外部服务，不读取或输出凭据、正文、Mongo 草稿、revision pointer 或完整生产日志。回滚点为每批文件的提交前版本；如类型标注暴露现有调用问题，先回退该批，再单独修正真实接口，不改变受保护数据。

## 8. 测试命令

在 WSL2 的 `wagtailblog-test` 环境执行：

```bash
python -m compileall wagtailblog3/apps/blog/services/markdown_import_parser.py wagtailblog3/apps/blog/services/markdown_import_service.py wagtailblog3/apps/search/services/rebuild.py wagtailblog3/apps/search/services/delivery.py
python manage.py check
python manage.py test blog.test_markdown_import_parser blog.test_markdown_import_service search.tests.test_search_rebuild search.tests.test_search_sync_delivery
git diff --check
```

若测试模块名称或数据库依赖与当前工作树不一致，以 `rg --files` 和实际测试收集结果为准，并在实施记录中说明替代命令。

## 9. 模型/推理强度建议与实际使用

- 事实收集和文档整理：Luna，低到中推理；门禁是现有代码、调用点和测试证据。
- 局部 Django/Wagtail 类型标注和注释：Terra，中推理；门禁是行为测试不变。
- 只有类型变化影响跨服务契约、并发状态、生产数据或回滚时才升级 Sol，高推理；首批方案暂不触发。

本次实际使用当前会话可用模型完成技能核对和方案整理，未调用外部模型，也未发送源码、凭据或生产数据。

## 10. 实施记录

### 2026-08-24：绑定员工角色与后端交付门禁

- 状态：完成，未修改运行时代码。
- 实际修改文件：本方案新增“员工执行绑定”小节；协作映射见 `说明书/24-多agent协作与模型技能调度方案.md`，仓库强制规则见 `AGENTS.md`。
- 修改内容：明确所有实际修改运行时代码的 agent 均须按本方案补充中文注释、准确类型和验证证据；将缺少契约或测试证据定义为不可合并状态。
- 验证：UTF-8 文档读取、关键条款检索和 `git diff --check` 通过；未运行 Django 或服务测试，因为本批次只改变协作文档。
- 数据/服务影响：无数据库、MongoDB、Elasticsearch、Redis、媒体、队列、systemd 或生产数据操作；`systemctl.md` 无需更新。
- 回滚点：删除本小节及对应协作文档条款即可恢复原流程；不影响既有代码批次。
- 残余风险：后续每个后端代码批次仍需提供实际注释覆盖和测试证据，规范本身不能替代审查。

### 2026-08-22：批次 2B Terra 高风险搜索文档/建议/标题索引完成

- 状态：完成，未提交 commit。
- 实际修改文件：`search/services/document.py`、`search/services/suggestions.py`、`search/services/title_index.py`。
- 修改内容：补充正式 Mongo 正文投影、内容哈希、批量文档构造、建议文本安全边界、ES alias 读取与 live/public 二次校验、标题索引 strict mapping 和创建后校验的中文说明与可确认类型。
- 运行时行为：未修改 Mongo 正文读取顺序、哈希字段排除规则、建议去重、ES 查询结构、索引 mapping 或异常编码。
- 测试结果：Windows `compileall` 和 `git diff --check` 通过；WSL2 `wagtailblog-test` `python manage.py check` 通过；定向测试 33 个全部通过（`OK`）。
- 已知警告：测试环境保留项目原有 `wagtailcore.WorkflowState` 条件唯一约束警告；与本批注释/类型标注无关。
- 数据/服务影响：未创建、删除或切换真实 Elasticsearch 索引/alias，未写入 MongoDB、MySQL 或 Redis，未重启 systemd 服务；`systemctl.md` 无需更新。
- 回滚点：恢复上述三个文件即可回滚本批；Luna 批次、第一批文件和方案文档的未提交修改保留不变。
- 残余风险：批次 2 其余 Terra 文件（alias、bootstrap、consistency、content_index、content_query、cursor、elasticsearch、federated_query、mongo、outbox、API、core、models、signals、tasks、views）仍待后续处理。

## 11. 后续文件计划（2026-08-22 梳理）

### 扫描基线（2026-08-22）

对 `wagtailblog3/apps` 进行只读 AST 扫描，排除 `*/migrations/*.py`、测试文件、`*/tests/*.py` 和 `__init__.py`：

- 范围：160 个运行时代码文件。
- 函数/方法：1,015 个，其中 648 个缺少完整参数或返回类型标注，505 个缺少文档字符串。
- 类：318 个，其中 142 个缺少类职责文档。
- 模块：71 个缺少模块级文档字符串。
- 缺口集中模块：`blog/models.py`（43 个函数/方法、42 个缺少完整标注、43 个类）、`blog/markdown_import_api.py`（30、28、14 个类）、`blog/markdown_renderer.py`（23、22）、`blog/feeds.py`（22、21）、`observability/views.py`（19、16）和 `blog/services/markdown_import_media.py`（22、18、3 个类）。这些文件分别涉及 Wagtail 模型、导入 API、Markdown 渲染、Feed、日志后台和媒体处理，应在后续批次按调用点与测试逐个处理。

这些数字是本轮规划基线，不作为修改业务代码的授权；每批开始前按当前工作树重新扫描。后续优先处理跨服务、权限、状态机、解析器、批量写入、管理命令和 Wagtail 模型/后台入口，再处理简单展示和框架注册代码。所有文件均按 3.4 的 `admin_image_upload.py` 范例补齐到足以审阅真实契约的程度，而不是机械追求每一行都有注释。

以下清单是 `wagtailblog3/apps` 后续注释和类型标注的范围。每批完成后单独运行相关测试并追加实施记录；不会一次性改完整个目录。

### 批次 2：搜索服务剩余链路

优先处理跨 MongoDB、Elasticsearch、Redis 和前台查询的状态/安全边界：

- `search/services/alias.py`
- `search/services/bootstrap.py`
- `search/services/consistency.py`
- `search/services/content_index.py`
- `search/services/content_query.py`
- `search/services/cursor.py`
- `search/services/document.py`
- `search/services/elasticsearch.py`
- `search/services/federated_query.py`
- `search/services/highlights.py`
- `search/services/mongo.py`
- `search/services/outbox.py`
- `search/services/pages_query.py`
- `search/services/suggestions.py`
- `search/services/title_index.py`

另含搜索运行时入口：`search/api.py`、`search/core.py`、`search/analytics.py`、`search/cache.py`、`search/signals.py`、`search/tasks.py`、`search/views.py`、`search/wagtail_hooks.py`、`search/urls.py`、`search/models.py`、`search/apps.py`。

#### 批次 2A：Luna 对话范围（低/中推理）

Luna 只处理不拥有索引写入、跨服务状态或安全决策的本地查询/展示辅助代码：

- `search/services/highlights.py`
- `search/services/pages_query.py`
- `search/analytics.py`
- `search/cache.py`
- `search/wagtail_hooks.py`
- `search/urls.py`
- `search/apps.py`
- `search/templatetags/search_tags.py`

Luna 任务边界：只增加准确的类型标注和中文注释；不得改变排序、过滤、缓存键、返回结构、模板上下文或公开 URL。复杂度升级条件：发现 ES 请求、游标签名、MongoDB 读取、写入操作、权限判断或模型状态迁移时，停止并转交 Terra。

Luna 验收：目标文件 `compileall`、`python manage.py check`、`search.tests.test_search_correctness`、`search.tests.test_search_wp5`、`search.tests.test_search_wp5_title_index`、`git diff --check`。实际测试模块以改动调用链为准，不能为通过测试而修改断言。

#### 批次 2B：Terra 对话范围（中推理）

Terra 处理索引、别名、内容版本、游标安全、异步投递、跨服务一致性和公共搜索入口：

- `search/services/document.py`
- `search/services/alias.py`
- `search/services/bootstrap.py`
- `search/services/consistency.py`
- `search/services/content_index.py`
- `search/services/content_query.py`
- `search/services/cursor.py`
- `search/services/elasticsearch.py`
- `search/services/federated_query.py`
- `search/services/suggestions.py`
- `search/services/title_index.py`
- `search/services/mongo.py`
- `search/services/outbox.py`
- `search/api.py`
- `search/core.py`
- `search/models.py`
- `search/signals.py`
- `search/tasks.py`
- `search/views.py`

Terra 任务边界：补充稳定错误编码、事务/版本不变量、ES/Mongo 边界、游标签名和 Outbox 幂等语义的中文说明与可确认类型。不得创建、删除或切换索引、alias、Target、Build、Outbox、Delivery 或 State；不得修改生产配置、服务、迁移或数据。

Terra 验收：目标文件 `compileall`、`python manage.py check`、`search.tests.test_search_content_index`、`search.tests.test_search_correctness`、`search.tests.test_search_sync_elasticsearch`、`search.tests.test_search_sync_models`、`search.tests.test_search_sync_producer`、`search.tests.test_search_sync_delivery`、`search.tests.test_search_wp4c`、`search.tests.test_search_wp5`、`search.tests.test_search_wp6`、`search.tests.test_search_wp7a`、`git diff --check`。涉及模型但不变更字段时仍执行 `makemigrations --check --dry-run`，不执行迁移。

执行顺序：先完成 2A，再完成 2B；两个对话不能并发写同一共享工作树。每个对话完成后都必须检查 `git status --short --branch`，保留另一对话和第一批尚未提交的修改。

### 批次 3：Markdown 导入剩余服务

在已完成 parser/service 基础上，继续覆盖鉴权、路径、远程读取、媒体和会话状态：

- `blog/services/markdown_import_auth.py`
- `blog/services/markdown_import_idempotency.py`
- `blog/services/markdown_import_media.py`
- `blog/services/markdown_import_paths.py`
- `blog/services/markdown_import_prepare.py`
- `blog/services/markdown_import_remote.py`
- `blog/services/markdown_import_sessions.py`
- `blog/services/markdown_import_types.py`

验收重点是 token 鉴权、幂等键、文件路径限制、远程资源超时、媒体补偿和 session 状态机；不得输出凭据、正文或 Mongo 指针。

### 批次 4：Observability 日志链路

这是独立的生产日志和 Elasticsearch 链路，放在搜索与导入稳定后处理：

- `observability/config.py`
- `observability/cleanup.py`
- `observability/elasticsearch_logs.py`
- `observability/filters.py`
- `observability/forms.py`
- `observability/helpers.py`
- `observability/pagination.py`
- `observability/parser.py`
- `observability/permissions.py`
- `observability/reader.py`
- `observability/registry.py`
- `observability/sanitizer.py`
- `observability/services.py`
- `observability/tasks.py`
- `observability/views.py`
- `observability/wagtail_hooks.py`
- `observability/urls.py`
- `observability/models.py`
- `observability/apps.py`
- `observability/management/commands/index_log_files.py`
- `observability/management/commands/prepare_log_index.py`
- `observability/management/commands/render_filebeat_config.py`
- `observability/management/commands/retry_log_index_sync.py`

验收重点是脱敏、权限、日志读取范围、ES 写入时序和 Filebeat/systemd 影响；涉及服务配置时必须另行更新 `systemctl.md`，本批注释本身不修改服务。

### 批次 5：Blog 核心运行时

- `blog/models.py`
- `blog/blocks.py`
- `blog/forms.py`
- `blog/views.py`
- `blog/markdown_import_api.py`
- `blog/markdown_extensions.py`
- `blog/markdown_renderer.py`
- `blog/middleware.py`
- `blog/ai_metadata.py`
- `blog/ai_metadata_views.py`
- `blog/analytics_views.py`
- `blog/admin.py`
- `blog/admin_image_upload.py`
- `blog/embeds.py`
- `blog/feed_urls.py`
- `blog/feeds.py`
- `blog/image_formats.py`
- `blog/inline_title_renderer.py`
- `blog/page_view_counter.py`
- `blog/signals.py`
- `blog/tasks.py`
- `blog/urls.py`
- `blog/wagtail_hooks.py`
- `blog/widget_adapters.py`
- `blog/widgets.py`
- `blog/services/content_analytics.py`
- `blog/services/feed_analytics.py`
- `blog/services/feed_cache.py`
- `blog/services/feed_query.py`
- `blog/services/tag_listing.py`
- `blog/templatetags/blog_tags.py`

模型、StreamField、Wagtail hooks 和后台入口必须保持现有序列化、权限、revision 和页面树行为不变；涉及浏览器交互的 hooks/widgets 另做浏览器验收。

### 批次 6：基础应用、评论、归档、首页、作品集和 AI

- `base/models.py`、`base/blocks.py`、`base/views.py`、`base/utils.py`、`base/tasks.py`、`base/admin.py`、`base/apps.py`、`base/services/social_links.py`、`base/templatetags/navigation_tags.py`
- `comments/models.py`、`comments/forms.py`、`comments/markdown.py`、`comments/views.py`、`comments/admin.py`、`comments/wagtail_hooks.py`、`comments/urls.py`、`comments/apps.py`、`comments/templatetags/comment_tags.py`
- `archive/models.py`、`archive/views.py`、`archive/admin.py`、`archive/wagtail_hooks.py`、`archive/urls.py`、`archive/apps.py`、`archive/services/listing.py`、`archive/templatetags/archive_tags.py`
- `home/models.py`
- `portfolio/models.py`、`portfolio/blocks.py`、`portfolio/views.py`、`portfolio/admin.py`、`portfolio/apps.py`
- `content_ai/models.py`、`content_ai/apps.py`、`content_ai/wagtail_hooks.py`、`content_ai/services/blog_metadata.py`

### 批次 7：管理命令

对会写入数据库、媒体、日志索引或搜索索引的命令单独补充“参数、dry-run、确认门禁、数据影响和回滚”说明：

- `base/management/commands/email_status.py`
- `base/management/commands/test_email.py`
- `blog/management/commands/add_blog_page.py`
- `blog/management/commands/backfill_pageview_visitor_keys.py`
- `blog/management/commands/clean_logs.py`
- `blog/management/commands/cleanup_pageviews.py`
- `blog/management/commands/create_bucket.py`
- `blog/management/commands/seed_content_analytics_demo.py`
- `blog/management/commands/sync_pageviews.py`
- `blog/management/commands/view_logs.py`
- `search/management/commands/*.py`（逐个核对生产确认和只读默认行为）

## 12. 明确排除项

- 所有 `*/migrations/*.py`：迁移文件是历史 schema，不做注释重写。
- 所有 `test_*.py`、`tests.py`、`*/tests/*.py`：测试说明随对应运行时代码批次维护，除非后续发现测试本身缺少关键场景。
- 所有 `__init__.py`：除非存在实际运行逻辑。
- `templates/`、静态资源、生成的配置和 `output/playwright/`：不属于本次 Python 注释计划。
- 生产环境文件、凭据、日志、媒体、Mongo 正文和 Elasticsearch 数据：不读取、不提交、不修改。

## 13. 每批统一验收

每个批次都执行：`python -m compileall`、`python manage.py check`、对应测试、`git diff --check`；涉及模型/迁移时追加 `python manage.py makemigrations --check --dry-run` 和 `python manage.py migrate --plan`。涉及 Wagtail 管理界面或前台交互时追加桌面/移动浏览器验收。所有批次只改注释和类型标注，不改变服务拓扑，除非方案另行记录并获得授权。

### 2026-08-22：第一批运行时代码注释与类型标注完成

### 2026-08-22：批次 2A Luna 低风险搜索辅助完成

- 状态：完成，未提交 commit。
- 实际修改文件：`search/services/highlights.py`、`search/services/pages_query.py`、`search/analytics.py`、`search/cache.py`、`search/templatetags/search_tags.py`、`search/wagtail_hooks.py`、`search/urls.py`、`search/apps.py`。
- 修改内容：补充高亮安全转换、公开页面 QuerySet、统计聚合、缓存版本隔离、模板过滤器、Wagtail 管理 URL、路由和应用生命周期的中文说明与可确认类型。
- 分类修正：`search/services/document.py`、`search/services/suggestions.py`、`search/services/title_index.py` 会读取 MongoDB/ES 或创建索引，已从 Luna 移交 Terra；后续不得由低推理批次修改。
- 测试结果：Windows `compileall` 和 `git diff --check` 通过；WSL2 `wagtailblog-test` `python manage.py check` 通过；定向测试 30 个全部通过（`OK`）。
- 数据/服务影响：未创建或删除索引，未写入 MongoDB/Elasticsearch/Redis，未修改 URL 行为、缓存键或返回结构，未重启 systemd 服务；`systemctl.md` 无需更新。
- 回滚点：恢复本批 8 个文件即可回滚；第一批四个文件和方案文档的未提交修改保留不变。
- 残余风险：搜索服务剩余跨服务文件必须使用 Terra；本批未覆盖 `document.py`、`suggestions.py`、`title_index.py`、`alias.py` 等高风险模块。

- 状态：第一批完成，未提交 commit。
- 实际修改文件：
  - `wagtailblog3/apps/blog/services/markdown_import_parser.py`
  - `wagtailblog3/apps/blog/services/markdown_import_service.py`
  - `wagtailblog3/apps/search/services/rebuild.py`
  - `wagtailblog3/apps/search/services/delivery.py`
- 修改内容：新增模块、类和复杂函数的中文说明；补充关键 helper 的参数/返回类型；解释图片偏移倒序替换、媒体组装错误边界、ES bulk 字节切分、checkpoint 原子推进、Delivery 租约、指数退避和 Outbox 状态聚合。
- 运行时行为：未修改查询、事务边界、异常编码、外部写入顺序或服务配置。
- 测试结果：
  - Windows `python -m compileall`：通过。
  - WSL2 `wagtailblog-test` `python manage.py check`：通过，无 error。
  - WSL2 定向测试：37 tests，全部通过（`OK`）。
  - `git diff --check`：通过。
- 已知警告：测试环境保留项目原有 `wagtailcore.WorkflowState` 条件唯一约束警告；与本批注释/类型标注无关，未修改。
- 数据/服务影响：无迁移、无数据库/MongoDB/Elasticsearch/媒体/队列写入；未启动、停止或重启 systemd 服务；`systemctl.md` 无需更新。
- 回滚点：恢复上述四个文件即可回滚本批；方案文档同步记录可单独恢复。
- 残余风险：部分历史 docstring 存在既有编码乱码，本批仅新增 UTF-8 中文说明，未对旧文本做全文件编码重写；后续批次仍需继续覆盖其他运行时模块。

### 2026-08-22：方案补充，未修改业务代码

- 状态：方案已补充，首批代码尚未开始。
- 实际修改：本方案文件；未修改 `wagtailblog3/apps`、`systemctl.md`、配置、迁移或测试。
- 核对结果：已读取 Django/Wagtail 开发技能、Karpathy 指南及 `systemctl.md`；确认本任务不改变服务拓扑和重启要求。
- 测试：未执行代码测试；下一步按第 8 节执行首批检查。
- 数据/服务影响：无数据库、MongoDB、Elasticsearch、队列、媒体或 systemd 操作。
- 回滚点：删除或恢复本方案文件即可回退本次文档变更；代码批次仍需独立记录。
- 残余风险：首批文件仍缺少完整的参数/返回文档，后续必须按分批范围继续实施。

### 2026-08-22：Terra 批次 2 搜索服务注释与类型标注（部分完成）
- 状态：完成本轮搜索服务文件，未提交 commit。
- 实际修改文件：`search/services/alias.py`、`bootstrap.py`、`consistency.py`、`content_index.py`、`content_query.py`、`cursor.py`、`elasticsearch.py`、`mongo.py`、`outbox.py`、`federated_query.py`。
- 修改内容：补充中文职责说明与函数输入/输出类型标注；明确 alias 原子切换并发保护、Page 行锁与 State 版本不变量、ES external version、Mongo 正文只读边界、签名游标查询绑定与过期策略、联合查询游标状态及 Outbox `on_commit` 唤醒语义。未改变查询 DSL、事务顺序、异常编码或返回结构。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/search` 与 `git diff --check` 通过；WSL2 `wagtailblog-test` `python manage.py check` 通过；使用 `--keepdb` 运行四个搜索同步/索引测试模块共 45 个测试全部通过。首次不带 `--keepdb` 因测试数据库已存在触发删除确认，未执行删除。
- 数据与服务影响：未创建、删除或切换 Elasticsearch index/alias，未写入 MongoDB/Redis/MinIO，未执行生产数据库操作，未启动或重启 systemd 服务；`systemctl.md` 无需更新。
- 回滚点：恢复上述 10 个代码文件即可回滚本批次；方案文档记录可独立恢复。
- 残余风险：`api.py`、`core.py`、`models.py`、`signals.py`、`tasks.py`、`views.py` 及管理命令等搜索运行时入口仍待后续批次处理；工作区保持未提交状态。

### 2026-08-22：Terra 批次 2 搜索入口注释与类型标注（本轮完成）
- 状态：完成指定的 `search/core.py`、`search/evaluation.py`、`search/views.py`；同时修复上一轮 `search/signals.py` 缺失 `Any` 导入的问题，未提交 commit。
- 修改内容：补充搜索核心分流、Wagtail 公开 QuerySet 二次过滤、ES 高亮安全边界、评测指标输入约束、游标/offset 分页校验、HTML/AJAX/JSON 响应契约的中文说明，并为函数、结果适配器和响应入口补充输入/输出类型。未改变搜索 DSL、路由、错误码、分页行为或数据写入顺序。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/search/core.py wagtailblog3/apps/search/evaluation.py wagtailblog3/apps/search/views.py wagtailblog3/apps/search/signals.py` 通过；`git diff --check` 通过；WSL2 `wagtailblog-test` `python manage.py test --keepdb search.tests.test_evaluation_metrics search.tests.test_search_correctness search.test_search_async search.tests.test_search_wp5 search.tests.test_search_wp4c search.tests.test_search_wp7a` 共 59 个测试全部通过（`OK`）。
- 已知警告：测试环境保留项目原有 `wagtailcore.WorkflowState` 条件唯一约束警告；与本批注释/类型标注无关，未修改。
- 数据与服务影响：未执行 Elasticsearch/MongoDB/Redis/MinIO 写入、索引或 alias 操作，未修改生产配置、迁移、systemd 服务或 `systemctl.md`。
- 回滚点：恢复本轮三个指定文件及 `signals.py` 的导入改动即可回滚本轮；其余既有未提交批次保持不变。

### 2026-08-22：search URL 路由逐条注释
- 状态：完成 `wagtailblog3/apps/search/urls.py` 注释，未提交 commit。
- 修改内容：为 `path` 导入、视图/API 导入、`app_name` 和每条搜索路由补充中文说明，明确 `/search/` 页面、`/search/results/` 结果片段、`/search/suggestions/` 传统联想、`/search/api/` 主 REST 搜索和 `/search/api/suggestions/` REST 联想的请求用途与响应方向。未修改 URL 路径、路由名称、顺序或视图绑定。
- URL 文件盘点：项目当前共有根路由、`archive/urls.py`、`blog/urls.py`、`comments/urls.py`、`observability/urls.py`、`search/urls.py` 6 个 URL 文件；其余 5 个文件尚未按本规则逐条注释。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/search/urls.py` 与 `git diff --check` 通过；WSL2 `python manage.py check` 通过；搜索路由/视图定向测试 24 个全部通过。
- 数据与服务影响：仅修改 Python 注释，不涉及数据库、MongoDB、Elasticsearch、Redis、媒体、队列或 systemd；`systemctl.md` 无需更新。
- 回滚点：恢复 `search/urls.py` 即可回滚本批次。

### 2026-08-22：全部项目 URL 路由逐条注释
- 状态：完成根路由和其余 5 个 URL 文件的路由用途注释，未提交 commit。
- 实际修改文件：`wagtailblog3/urls.py`、`wagtailblog3/apps/archive/urls.py`、`wagtailblog3/apps/blog/urls.py`、`wagtailblog3/apps/comments/urls.py`、`wagtailblog3/apps/observability/urls.py`、`wagtailblog3/apps/search/urls.py`。
- 修改内容：为导入、命名空间、每条 `path/re_path`、应用 `include`、参数含义、页面/API/AJAX/管理后台转发目标补充中文说明；明确根路由的语言前缀、开发静态/媒体服务、图片附件正则路由和 Wagtail 最后通配路由边界。未修改任何路径、路由名称、命名空间、顺序或视图绑定。
- 验证：Windows 6 个 URL 文件 `compileall` 通过，`git diff --check` 通过；WSL2 `python manage.py check` 通过。
- 数据与服务影响：仅修改 URL 文件注释，不涉及数据库、MongoDB、Elasticsearch、Redis、媒体、队列或 systemd；`systemctl.md` 无需更新。
- 回滚点：恢复上述 6 个 URL 文件即可回滚本批次；其余运行时代码未因本批次改变。

### 2026-08-22：博客后台 Vditor 图片上传注释与类型标注

- 状态：完成，未提交 commit。
- 目标：为错误响应、collection 权限选择、标题去重、Wagtail 表单保存、预览 rendition 和公开上传入口补充中文职责说明；为函数参数和返回值补充类型标注；解释随机后缀重试、单调时钟耗时统计、权限双重保护及预览失败补偿边界。
- 实际修改文件：`wagtailblog3/apps/blog/admin_image_upload.py`。
- 约束：不改变现有请求参数、错误码、HTTP 状态、表单校验与保存顺序、预览响应结构或日志字段；不执行真实上传，不写入数据库、媒体存储或外部服务。
- 修改内容：模块和函数补充中文 docstring；函数签名增加参数/返回类型；说明 collection 权限选择优先级、标题去重候选算法和五次重试上限、`time.monotonic` 耗时统计、Wagtail 表单校验边界、原图与 rendition 的保存顺序、预览失败不触发重复上传的补偿原则，以及管理访问和 POST 方法双重保护。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/blog/admin_image_upload.py` 通过；Windows `git diff --check` 通过；WSL2 `wagtailblog-test` `python manage.py check` 通过；`python manage.py test --keepdb blog.test_admin_image_upload` 共 12 个测试全部通过（`OK`）。
- 数据/服务影响：仅修改 Python 注释和类型标注，不修改 `systemctl.md`、配置、迁移、队列、端口或服务状态。
- 回滚点：恢复 `wagtailblog3/apps/blog/admin_image_upload.py` 即可回滚本批次；本方案文档记录可独立恢复。工作区其他批次的未提交修改不属于本批回滚范围。
- 未执行：未执行真实上传、媒体写入、MongoDB/Elasticsearch/Redis 写入、生产部署或 systemd 操作。
- 模型/推理强度实际使用：当前会话使用可用模型完成 Django/Wagtail 代码核对与局部注释；未调用外部模型，不发送源码、凭据或生产数据。

### 2026-08-22：建立 `admin_image_upload.py` 注释范例与全量扫描基线

- 状态：完成方案补充，未修改其他业务代码，未提交 commit。
- 实际修改文件：`说明书/22-运行时代码注释与类型标注方案.md`。
- 修改内容：将 `blog/admin_image_upload.py` 固化为后续模块的结构与深度范例，明确模块说明、函数签名、参数/返回/异常/副作用、算法不变量、保存补偿边界和必要行内注释的写作要求；补充排除测试/迁移后的 AST 扫描基线（160 个文件、1,015 个函数/方法、318 个类）及当前类型/文档缺口统计。
- 验证：只读 AST 扫描完成；`git diff --check` 通过。方案文档变更未触发 Django、数据库或浏览器测试。
- 数据/服务影响：未修改 `wagtailblog3/apps` 业务代码、`systemctl.md`、配置、迁移、队列、端口或服务状态；未读取或写入生产数据。
- 回滚点：恢复本方案文档本次新增的 3.4 范例规范、扫描基线和本实施记录即可回滚本次规划变更；此前所有未提交代码批次保持不变。
- 残余风险：扫描统计会随工作树变化，后续批次开始前必须重新计算；统计结果不替代逐文件调用点、测试和数据边界审阅。

### 2026-08-22：Markdown 导入基础服务注释与类型标注（第一小批）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/services/markdown_import_auth.py`、`markdown_import_idempotency.py`、`markdown_import_paths.py`、`markdown_import_prepare.py`、`markdown_import_types.py`。
- 修改内容：按 `admin_image_upload.py` 范例补充模块/类/函数中文说明和函数签名类型；说明明文密钥不落库、幂等 UUIDv4、并发唯一键竞态、路径编码解码与目录穿越防护、媒体计划去重冲突、解析对象生命周期。
- 验证：Windows 目标文件 `compileall` 通过，`git diff --check` 通过；WSL2 `python manage.py check` 通过；`blog.test_markdown_import_auth blog.test_markdown_import_paths blog.test_markdown_import_prepare blog.test_markdown_import_batch` 共 24 个测试全部通过。
- 数据/服务影响：未执行真实导入、数据库/媒体/外部服务写入或 systemd 操作；`systemctl.md` 无需更新。
- 回滚点：恢复上述 5 个文件即可回滚本小批；此前未提交批次保持不变。

### 2026-08-22：Markdown 导入远程下载与会话状态注释（第二小批）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/services/markdown_import_remote.py`、`markdown_import_sessions.py`。
- 修改内容：补充远程 HTTPS、DNS 公网地址、重定向、流式大小限制、图片解码和临时文件清理的算法说明；补充会话终态、行锁竞争、页面/Mongo/媒体补偿和 cleanup retry 边界说明。
- 验证：Windows 目标文件 `compileall` 通过，`git diff --check` 通过；WSL2 `blog.test_markdown_import_remote blog.test_markdown_import_media blog.test_markdown_import_tasks blog.test_markdown_import_service` 共 56 个测试全部通过。
- 数据/服务影响：未执行真实远程下载、媒体写入、MongoDB/Elasticsearch/Redis 写入或生产服务操作；`systemctl.md` 无需更新。
- 回滚点：恢复上述 2 个文件即可回滚本小批；其他未提交修改不属于本批回滚范围。
- 残余风险：`markdown_import_media.py` 仍有较多媒体探测、存储持久化和清理函数待按同等深度补充；这是下一小批范围，不能以本记录代替完成声明。

### 2026-08-22：Markdown 导入媒体探测与持久化注释（第三小批）

- 状态：完成，未提交 commit；批次 3 的 8 个剩余服务文件全部完成。
- 实际修改文件：`blog/services/markdown_import_media.py`。
- 修改内容：补充 MP3 帧长度和连续帧确认、MP4 box 边界与轨道递归解析、媒体内容探测、深度 MIME/编解码器校验、精确 storage/model 清理、对象名长度限制、分块 SHA-256、并发名称碰撞、模型保存失败和 cleanup retry 的中文说明与类型标注。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/blog/services/markdown_import_media.py` 通过；`git diff --check` 通过；WSL2 `blog.test_markdown_import_media blog.test_markdown_import_service blog.test_markdown_import_tasks` 共 50 个测试全部通过。
- 数据/服务影响：未执行真实媒体上传、对象存储删除、数据库/MongoDB/Redis 写入或生产服务操作；`systemctl.md` 无需更新。
- 回滚点：恢复 `markdown_import_media.py` 即可回滚本小批；前两小批和其他未提交修改保持不变。

### 2026-08-22：Blog Markdown 与标题渲染注释（批次 5A）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/markdown_renderer.py`、`blog/inline_title_renderer.py`。
- 修改内容：补充函数/方法类型标注和中文说明；解释旧列表缩进修复只作用渲染副本、Wagtail 页面/图片 embed 白名单、原生 HTML 公式节点处理、nh3 清理顺序、标题单行/HTML/代码块/展示公式限制，以及高亮不穿透公式和 HTML 标签的边界。
- 验证：Windows 目标文件 `compileall` 通过，`git diff --check` 通过；WSL2 `blog.test_formatted_titles blog.test_markdown_compat blog.test_markdown_import_parser` 共 59 个测试全部通过。
- 数据/服务影响：未修改原始 Markdown、数据库、媒体、搜索索引或服务配置；未执行生产服务操作，`systemctl.md` 无需更新。
- 回滚点：恢复上述 2 个文件即可回滚本小批；其他批次未提交修改保持不变。

### 2026-08-22：Blog 标签查询注释（批次 5B）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/services/tag_listing.py`。
- 修改内容：补充模块说明、日期过滤输入/输出类型、标签列表与标签详情分支、live/public 可见性边界和分页查询原理。
- 复核保持不变：`blog/services/feed_cache.py`、`blog/services/feed_query.py` 已具备接近范例的中文职责说明、类型签名和缓存/权限边界，本批未产生无意义重写。
- 验证：Windows `compileall` 和 `git diff --check` 通过；WSL2 `blog.test_tag_index_async` 共 11 个测试全部通过。
- 数据/服务影响：只读查询说明变更，不写入数据库、缓存、媒体或搜索服务；`systemctl.md` 无需更新。
- 回滚点：恢复 `tag_listing.py` 即可回滚本小批。

### 2026-08-22：Markdown 导入 API 注释与类型标注（批次 3D）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/markdown_import_api.py`。
- 修改内容：补充模块、异常类、helper 和所有 API view 的中文职责与参数/返回类型；详细说明目标页面权限、块/日期/标签校验、artifact 清单双向覆盖、请求指纹幂等、session 行锁与单文件上传、AI 建议只读边界、同步导入补偿顺序及 DRF 异常响应协商。
- 验证：Windows `compileall` 和 `git diff --check` 通过；WSL2 `python manage.py check` 通过；`blog.test_markdown_import_api blog.test_markdown_import_auth blog.test_markdown_import_batch blog.test_markdown_import_tasks` 共 39 个测试全部通过。
- 数据/服务影响：未执行真实 Markdown 导入、页面/Mongo/媒体写入、索引或生产服务操作；`systemctl.md` 无需更新。
- 回滚点：恢复 `markdown_import_api.py` 即可回滚本批；此前所有 Markdown 服务批次保持不变。

### 2026-08-22：Blog RSS/Atom Feed 注释与类型标注（批次 5C）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/feeds.py`。
- 修改内容：补充 Feed 基类和范围 Feed 的中文职责、请求方法限制、查询参数规范化、缓存命中/生成、ETag/Last-Modified 条件响应、HEAD 空正文、公开文章元数据和频道 GUID 说明；为方法补充参数/返回类型。
- 验证：Windows `compileall` 和 `git diff --check` 通过；WSL2 `python manage.py check` 通过；`blog.test_tag_index_async blog.test_markdown_compat` 共 46 个测试全部通过。
- 数据/服务影响：未写入数据库、正文、MongoDB、搜索索引或生产缓存；未修改服务配置，`systemctl.md` 无需更新。
- 回滚点：恢复 `feeds.py` 即可回滚本批。

### 2026-08-22：Blog AI 元数据生成注释与类型标注（批次 5D）

- 状态：完成，未提交 commit。
- 实际修改文件：`blog/ai_metadata.py`。
- 修改内容：补充建议数据类、文本提取、Markdown/rich text 清理、模型响应字段白名单、OpenAI Responses API 配置边界、`store=False` 隐私约束和最多三次 JSON 重试算法说明；为初始化和生成方法补充类型/职责说明。
- 验证：Windows `compileall` 和 `git diff --check` 通过；WSL2 `blog.test_ai_metadata blog.test_markdown_import_api` 共 32 个测试全部通过；保留项目原有 `WorkflowState` 条件唯一约束警告，未修改。
- 数据/服务影响：未调用真实 AI 服务，未发送正文、未写入页面/数据库/外部服务；`systemctl.md` 无需更新。
- 回滚点：恢复 `ai_metadata.py` 即可回滚本批。

### 2026-08-22：Blog models 统计、反应与导入模型注释（批次 5E）

- 状态：完成，未提交 commit。
- 实际修改文件：`wagtailblog3/apps/blog/models.py`。
- 修改内容：补充页面访问聚合、来源/订阅客户端、阅读会话、反应类型、反应记录、作者和 Markdown 导入状态模型的中文职责说明；为后台摘要、作者 slug/简介预览、Token 签发与有效性判断，以及类方法 `cls` 参数补充类型标注；明确匿名反应唯一性、历史统计字段兼容、Token 只存摘要和导入状态边界。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/blog/models.py`、`git diff --check` 通过；WSL2 `python manage.py check` 通过；相关 Markdown/博客测试共 68 个全部通过（`OK`）。
- 数据/服务影响：仅修改 Python 注释和类型标注，未生成迁移，未写入 MySQL、MongoDB、Elasticsearch、Redis 或媒体存储，未启动或重启 systemd 服务；`systemctl.md` 无需更新。
- 回滚点：恢复 `wagtailblog3/apps/blog/models.py` 本批新增注释和类型标注即可回滚；此前未提交批次保持不变。
- 残余风险：模型字段及历史编码文本未重写，后续仍需按批次继续覆盖 Blog 其余运行时代码并逐批运行针对性测试。

### 2026-08-22：Blog StreamField 块注释与类型标注（批次 5F）

- 状态：完成，未提交 commit。
- 实际修改文件：`wagtailblog3/apps/blog/blocks.py`。
- 修改内容：补充 Markdown、Mermaid、代码、文档、音频、视频、表格和嵌入块的中文职责说明；为块字段、渲染、旧数据兼容转换、视频 MIME 推断和表格模板上下文组装补充输入/输出类型；说明 Mermaid 缺省渲染器只在内存中兼容、媒体类型按元数据/扩展名回退的原因，以及表格合并元数据传递规则。
- 验证：Windows `python -m compileall -q wagtailblog3/apps/blog/blocks.py`、`git diff --check` 通过；WSL2 `python manage.py check` 通过；`blog.test_markdown_compat` 共 35 个测试全部通过（`OK`）。
- 数据/服务影响：仅修改 Python 注释和类型标注，未改变 StreamField 序列化、MongoDB 正文、模板路径、媒体存储或请求行为；未生成迁移，未操作外部服务和 systemd，`systemctl.md` 无需更新。
- 回滚点：恢复 `wagtailblog3/apps/blog/blocks.py` 本批新增内容即可回滚；模型批次 5E 及其他未提交修改保持不变。
- 残余风险：Blog 管理后台、视图、统计服务和管理命令仍有 AST 扫描出的签名缺口，后续按方案分批处理。

### 2026-08-22：注释批次提交与生产同步记录

- 状态：已提交并部署。
- Git：`bf53b978714de45bf4590b5adc3a845414eb84df`（主要注释批次）和 `971f1f8a1aa4aec533094fcc6ec0461813b9a9ea`（根 URL 注释补充）已提交并推送 `origin/main`。
- 生产同步：服务器 `192.168.20.2` 的 `main` 工作树原为干净旧 commit `3b8599f`，已通过 `fetch`、差异清单和 `merge --ff-only` 快进到 `971f1f8`；未执行迁移、collectstatic、数据写入或外部索引操作。
- 生产验收：生产 `manage.py check` 通过；`wagtailblog3.service`、maintenance Worker、Celery Beat、Filebeat 均 active/enabled；`systemctl --failed` 无失败 unit；`http://127.0.0.1:6050/admin/login/` 返回 HTTP 200；Git 工作树干净且与 `origin/main` 一致。
- 服务影响与回滚：本批只改注释、类型标注和方案文档，不改变运行逻辑，因此未重启服务；回滚点为生产上一 commit `3b8599f`，如需回滚只允许恢复代码后重新执行同等健康检查。
